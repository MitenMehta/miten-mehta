name: Retired three-zone Cloudflare apply

on: workflow_dispatch

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This cross-zone auto-apply workflow is retired. Use track-a-quality.yml and a reviewed, state-backed infrastructure promotion."
