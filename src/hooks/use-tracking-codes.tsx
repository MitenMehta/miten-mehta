/**
 * Dynamic database-supplied scripts are intentionally disabled.
 * Approved analytics must be integrated as reviewed static source code or
 * through a separately governed first-party tag-manager configuration.
 */
export const useTrackingCodes = (): void => undefined;
